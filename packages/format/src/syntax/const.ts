/**
 * Magic number of Resource Bundle as a string (to be UTF-8 encoded)
 */
export const magicNumber = "🌐📦";

/**
 * Version number of Resource Bundle
 */
export const versionNumber = Buffer.from([0x31, 0, 0, 0]);
